node_modules/
build/
dist/
coverage/
.DS_Store
*.log
tmp-*.err
.vercel/
.env*
!.env.example
.vercel

# Claude Code worktrees — never track these (they were once committed as
# bogus gitlinks; keep them out of the index for good)
.claude/worktrees/

# Local-only working folders (kept out of build & Tailwind content scan)
.figma-extract/
tmp-figma/
public-landing-v2/_copy_clain_filled.json
public-landing-v2/_copy_manifest.json
